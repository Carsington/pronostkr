<%= simple_form_for([@competition, @league]) do |f| %>
        <%= f.input :name, as: :string, label: 'Nom de ta ligue:', placeholder: "Team naze" %>
        <%= f.input :slug, as: :string, label: "Code secret de ta ligue:", placeholder: "3423" %>
        <%= f.input :competition, as: :string %>

        <%= f.submit "Créer ta ligue", class: "btn btn-primary btn-block" %>
      <% end %>
